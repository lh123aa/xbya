# 人工验收结论（P4-A3）

> 一条一行，格式：`<编号> <场景> | <判定> | <说明>`
> 判定词只认：`通过` / `部分通过` / `不通过` / `未做`
> 下面是**模板**，把 `未做` 改成你的实测判定，并在说明里写清现象。
> 全部填完后跑 `python tools/check_manual_evidence.py` 校验。

## 环境（做验收时请填，结论要能追溯到机器与设备）

```
日期：
机器 / 系统：
麦克风：
欣雅版本（git 提交）：
```

## 结论

<!-- 校验脚本会忽略含占位标记的行、以及判定为"未做"的行；
     所以照抄模板等于没填 —— 必须改成你自己的实测判定。 -->

```
M1 搜索文件 | 未做 | 待填
M2 删除确认 | 未做 | 待填
M3 打断     | 未做 | 待填
M4 降级路径 | 未做 | 待填
M5 安全边界 | 未做 | 待填
G1 GUI      | 未做 | 待填
```

## 观察到的其它问题（可选，但很有价值）

```
（主观感受、偶发现象、跟本手册无关的怪事，都写这里。不通过项尤其要写清"看到什么"）
```
