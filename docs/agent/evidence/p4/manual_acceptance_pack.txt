P4-A3 证据：人工验收执行包（包本身的自检 + 当前状态）
======================================================================
日期：2026-09-11
任务：docs/agent/tasks-p4.json §P4-A3
生成方式：本文件由三个脚本的真实输出拼成
复跑命令：python tools/check_manual_evidence.py
          python tools/prepare_manual_acceptance.py status

【零】这一项交付的是**执行包**，不是"验收通过"
--------------------------------------------------------------
麦克风采集、GUI 动效、人耳听感**只有人能判**。本文件的证据只证明两件事：
1. 执行包本身是完整可用的（手册齐全、沙箱能起、结论校验器会报红）；
2. **验收尚未执行**（结论文件里还是"未做"）。

> `docs/agent/manual-acceptance.md` 里写明：**本手册未执行完成之前，不得对外宣称产品可用。**
> 这句话是本条的判据核心 —— 包做完 ≠ 验收通过。

【一】当前状态（结论校验器）
--------------------------------------------------------------
========================================================================
人工验收证据校验（P4-A3）
========================================================================
证据目录：docs\agent\evidence\manual
提醒：本脚本只查「有没有」，不替你判「过不过」—— 判定权在人。

[缺  ] M1 搜索文件（确认语 ≤1.5s + 结果与磁盘一致）
         证据 0/1，结论未填（或只是占位/示例）
[缺  ] M2 删除确认（预览只列目标文件 + 一起进了回收站）
         证据 0/2，结论未填（或只是占位/示例）
[缺  ] M3 打断（停播报 + 不再播结果 + 不卡死）
         证据 0/1，结论未填（或只是占位/示例）
[缺  ] M4 降级路径（闲聊不被误判成工具）
         证据 0/1，结论未填（或只是占位/示例）
[缺  ] M5 安全边界（拒绝 + 理由可读 + 审计留痕）
         证据 0/2，结论未填（或只是占位/示例）
[缺  ] G1 GUI 观察点（动效 / 气泡 / 自动消失 / 大致的同步）
         证据 0/1，结论未填（或只是占位/示例）

------------------------------------------------------------------------
还差 6 条：M1, M2, M3, M4, M5, G1

怎么补：
  1. 照 docs/agent/manual-acceptance.md 做对应场景
  2. 证据放进本目录，文件名以编号开头（如 M2-删除-预览气泡.png）
  3. 在 结论.md 里为它补一行：`M2 ... | 通过 | 说明`

⚠️ 本包未完成前，不得宣称产品可用（麦克风采集 / GUI 动效 / 人耳听感只有人能判）。
========================================================================

【二】沙箱状态（`prepare_manual_acceptance.py status`）
--------------------------------------------------------------
沙箱模式: 关
配置文件  : E:\程序\桌面宠物\xiaoyi-vrm-worktree\config.yaml
备份存在  : False
沙箱存在  : False  (C:\Users\49046\AppData\Local\Temp\xiaoyi_manual)
白名单行  : path_whitelist: []

【三】校验器自检（证明它不会把"没做"读成"通过"）
--------------------------------------------------------------
本项目在 P0 阶段踩过两条同类坑，都在这个校验器上：
- 一条**假结论行**（`M1 ... | 通过 | 自检`）就能把它刷绿；
- **照抄模板**（结论列写"未做"）也算"已填"。

所以它必须用反方向输入验证过：

========================================================================
自检：判定逻辑两个方向都要成立
========================================================================
  PASS  真人写了通过                                     判定=True 期望=True
  PASS  真人写了不通过（诚实的失败也是完整结论）                       判定=True 期望=True
  PASS  部分通过                                       判定=True 期望=True
  PASS  模板原样：未做                                    判定=False 期望=False
  PASS  ★ 假结论 + 自检标记                               判定=False 期望=False
  PASS  ★ 假结论 + 示例标记                               判定=False 期望=False
  PASS  ★ 假结论 + 模板标记                               判定=False 期望=False
  PASS  根本没有这一行                                    判定=False 期望=False
  PASS  空文本                                        判定=False 期望=False
  PASS  只有编号没有判定词                                  判定=False 期望=False
  PASS  占位词在别的编号行上，不应误伤本行                          判定=True 期望=True
  PASS  正常证据文件                                     占位=False 期望=False
  PASS  文件名带假证据                                    占位=True 期望=True
  PASS  内容带占位                                      占位=True 期望=True
  PASS  内容带 TODO                                   占位=True 期望=True
  PASS  体积过小（疑似空文件）                                占位=True 期望=True
------------------------------------------------------------------------
自检通过：16/16 —— 会报红，也不乱报红
========================================================================

【四】交付物清单
--------------------------------------------------------------
| 文件 | 作用 |
|------|------|
| `docs/agent/manual-acceptance.md` | M1~M5 + G1，每项五列（你要做什么/期望现象/如何判定/证据放哪/失败时抓什么） |
| `tools/prepare_manual_acceptance.py` | `init`/`status`/`restore`/`cleanup`：建沙箱、临时改白名单、打印第 1 句 |
| `tools/check_manual_evidence.py` | 校验"每个场景都有证据 + 有真人结论"，缺哪个报哪个 |
| `docs/agent/evidence/manual/README.md` | 证据命名规范 |
| `docs/agent/evidence/manual/结论.md` | 结论模板（含环境栏） |

**配置可安全往返**已在执行时验证：`init` 改白名单后 `restore` 能回到**逐字节一致**
（SHA256 改前改后相同：`91F60DD2081BDD0045BFB5A9735D6476E094F3081B22524143B75A35300D99A2`）。
为什么必须先改白名单：`path_whitelist: []` 会用**用户真实的**四目录，
而"删除截图"场景的 `*截图*` 通配符会**同时命中用户自己的截图** ——
沙箱化是"能不能安全做这条验收"的前提。

【五】仍需人做的（唯一无法自动化的三件事里的一件）
--------------------------------------------------------------
1. 照 `docs/agent/manual-acceptance.md` 走一遍 M1~M5 + G1，留证到 `evidence/manual/`；
2. 在 `结论.md` 里逐条写真人结论（"通过/部分通过/不通过 + 现象"）。
