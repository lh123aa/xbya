桌宠换成照片复刻的卡通形象（2D 精灵图）+ 接线两处配置

形象
- resources/sprites/xinya/：从用户给的立绘抠图 → 裁上半身 → 缩放到 128×128，
  程序化生成 10 个动画共 208 帧
  （idle/talk/listen/think/happy/sad/sleep/stare/dance/wander）
- 立绘 1776×2256（rembg 抠图）→ 角色 88×110 放在 128 画布；
  脸 27×39、眼睛 y45~50、嘴 y58~68（逐像素核对，见 evidence/p5/_frame_landmark_check.png）
- 动画是「对一张图做局部变换」（呼吸/眨眼/口型），不是新表情图；
  做不到真正的表情变化，已在生成器 docstring 里写明前提

接线（修掉两项「配置项存在但从未接线」）
- D22：ui.render_mode 以前从没被读过，PetWindow 硬编码 sprite、
  app.py 无条件调 enable_vrm()。现按配置决定
- D23：角色名写死 load_pet("cat")。现读 ui.pet_sprite，
  换角色 = 改配置；测试断言从「写死 cat」改为「验证读配置」

顺带
- D21：发现 ConfigManager.set() 会整份落盘（app 启动时必调一次），
  我在测试里改内存字典时把测试值写进了真实 config.yaml；已恢复配置、
  测试改用 monkeypatch 拦读，并加 MD5 现场核对。根因待独立一轮修
- .gitignore 补 config.yaml.before_* 等备份（含明文密钥，G13 拦不住未跟踪文件）

验证
- 全量 pytest 2777 passed / 1 skipped / 0 failed（改动前同数，无回归）
- tools/verify_gui_launch.py 16/16；日志实测「精灵图角色: xinya / 10个动画」
- tools/check_repo_hygiene.py 21/22（唯一 FAIL 是「工作区有未提交改动」，即本次提交）
- 离线核对 10 个动画各加载到预期帧数、合成的帧非空且整段有真实运动
