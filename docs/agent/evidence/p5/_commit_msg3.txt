P5 收口：收口审计 + 13 关验收留证 + 账本对齐现实

一、P5-AUDIT-CLOSURE：把"闭环"做成可逐条核对的

  新增 docs/agent/evidence/p5/closure_audit.txt（由 _tmp_closure_audit.py 生成，
  **可复跑**）。它不产生结论，只列事实：
    · 逐条列 18 项的终态 + 已留档证据（逐条核对文件是否存在）+ 计划证据；
    · 数出终态分布；
    · 扫「以后再说 / 待定 / 后续再看」这类**非终态**措辞（正反两个方向都要防）；
    · 原样复跑 G13 与读最近一次 G2/G3 留档。

  它一跑就照出一处**账本与现实不一致**：AGENTS.md 的 P5 表里 P5-A1/A3/AUDIT
  都是 ✅ 完成，而 tasks-p5.json 里它们还是 pending —— **机器只读后者**。
  这正是本项目反复防的那类问题（"文档说做完了"≠"账本记着做完了"）。
  已用 _tmp_sync_p5_states.py 对齐（脚本会先验证证据文件存在，不存在就拒绝改状态）。

二、13 关验收（原始输出在 docs/agent/evidence/acceptance/ 与 SUMMARY.md）

  内部 6/7（G13 因**本轮改动尚未提交**、工作区不干净而红 —— 这是正确判据，
  提交后复跑转绿，两次原文都留在 p5/g13_hygiene.txt）；
  外部依赖 6/6 退出码 0，且本轮**零跳过**：
    G2 2668 passed / 1 skipped · G3 7238 语句 0 未覆盖 100% ·
    G4 18/18 · G5 44/44 · G6 指标 8/8 · G7 113/113 ·
    G8 20/20（0 跳过）· G9 35/35 · G10 32/32 · G11 16/16 · G12 50/50（失败 0 降级 0）。

三、完成度表按审计结果更正

  AGENTS.md「完成度」一节原先写 "P5 登记项 17 项"，实际 18 项（P5-AUDIT 与
  P5-AUDIT-CLOSURE 是执行中新增的），终态分布也改为按审计脚本数出来的数字：
  已修 2 / 已完成 3 / 明确不做 6 / 待人工 2 / 未到终态（P1 可选增强）5。
  并写明"上表的数字是审计脚本数出来的，不是我数出来的"。
