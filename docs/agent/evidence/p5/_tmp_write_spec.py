# -*- coding: utf-8 -*-
r"""生成**角色规格书**：`docs/agent/evidence/manual/角色规格书.md`

用途：用户要拿它去喂 AI 绘图工具（即梦 / Stable Diffusion / Midjourney 等），
生成"照片里这个人的卡通上半身形象"，再把成品接回桌宠。

本文件**不是**我画的图，是**绘图用的规格与提示词**。
我能做的是：把照片特征量准、整理成可执行的绘图指令、规划接回程序的路径。
画图这件事我做不了（没有图像生成能力）—— 这一点在文档里也写明，
以免日后有人误以为这份文档是我产出的形象。

关键约束（必须写进文档，否则生成结果会接不上）：
  · 必须是**上半身**、**正面或接近正面**、**背景透明或纯色**
    —— 因为桌宠要把它抠图后当精灵用
  · 头发**不要挡住肩线**太多，否则抠图后边缘会碎
  · 输出**分辨率要够**，因为要缩到 127px 显示、还要做眨眼/说话多帧
"""
import io
from pathlib import Path

ROOT = Path(__file__).resolve().parents[4]
OUT = ROOT / "docs" / "agent" / "evidence" / "manual" / "角色规格书.md"

# ── 配色（来源：_tmp_bust_palette.py 的迭代剔野值取色）──
PALETTE = [
    ("hair_base",   "#1C1618", "✔ 量出", "脸框内两侧头发，保留比例 72.6%（原值 #04070C，"
                                          "插画里提暖避免糊成一团）"),
    ("hair_mid",    "#2E2426", "~ 参考", "头发中间调 / 发束分界"),
    ("hair_hi",     "#4E3E3C", "~ 参考", "头发高光、发丝亮部"),
    ("skin",        "#E1C2B4", "✔ 量出", "颊部 #F2D3C5 与额部 #D0B1A3 的平均"),
    ("skin_shadow", "#C1A69A", "~ 参考", "颈部 / 下巴阴影"),
    ("blush",       "#E59F93", "~ 参考", "腮红"),
    ("dress",       "#D5D8D5", "✔ 量出", "白色连衣裙主体，保留比例 59.3%"),
    ("dress_shade", "#BBBEBB", "~ 参考", "裙褶阴影"),
    ("eye_iris",    "#4A3226", "✗ 推断", "**照片里眼睛太小，取色不可靠**，按观察取暖褐"),
    ("eye_deep",    "#120C0A", "✗ 推断", "瞳孔"),
    ("lips",        "#CB8478", "~ 参考", "**唇框保留比例仅 21%**，量出的 #DBB9A5 偏肤色，"
                                         "此处按观察压红"),
]

L = []
w = L.append

w("# 角色规格书 —— 上半身卡通形象")
w("")
w("> **这份文档是什么**：把用户提供的照片特征**量准并整理成绘图指令**，")
w("> 供 AI 绘图工具或画师使用。")
w(">")
w("> **这份文档不是什么**：它**不是形象本身**。画图这件事本项目的 AI 助手做不了")
w("> （环境里没有图像生成模型）。文档里不含任何由 AI 生成的角色图像。")
w("> 早期我曾尝试「用代码画」，产出见 `evidence/p5/_bust_sheet.png` ——")
w("> 那版是几何多边形拼的，棱角明显、比例生硬，**不足以作为成品**，仅作过程留档。")
w("")

w("---")
w("")
w("## 一、配色表（可直接填进绘图工具）")
w("")
w("| 变量 | HEX | 可信度 | 说明 |")
w("|------|-----|--------|------|")
for name, hexv, conf, note in PALETTE:
    w(f"| `{name}` | `{hexv}` | {conf} | {note} |")
w("")
w("**取色方法**（可复现）：OpenCV `haarcascade_profileface` 检测人脸定位（不靠目测坐标）→")
w("各部位小框多点采样 → 迭代剔野值（每轮以中位色为中心，剔除 RGB 距离 > 38 的像素）→")
w("取中位色。脚本：`evidence/p5/_tmp_bust_palette.py`。")
w("")
w("⚠️ **三个值是推断，不是测量**：`eye_iris` / `eye_deep` / `lips`。")
w("   照片里眼睛太小、嘴唇被高光冲淡，取色不可靠。")
w("   **对外陈述时不要把推断说成测量结果。**")
w("")
w("**只能当硬约束的是这三个**：`hair_base` · `skin` · `dress`。其余作为风格提示。")
w("")

w("---")
w("")
w("## 二、形象特征描述（按照片）")
w("")
w("| 部位 | 特征 |")
w("|------|------|")
w("| 发型 | **长卷发，过胸**；中分略偏，两侧波浪下垂；额前有**斜分刘海**，不厚重 |")
w("| 发色 | 近黑，带一点暖褐（强光下可见棕色反光） |")
w("| 脸型 | 鹅蛋脸，下巴略尖 |")
w("| 眼睛 | 黑褐色，眼神温和；**偏大**（卡通化时可按比例放大） |")
w("| 眉 | 细、平缓、颜色与发色同系 |")
w("| 妆容 | 淡；唇色自然偏红；脸颊有淡腮红 |")
w("| 服装 | **白色无袖连衣裙**；V 领；上身有细密竖纹（抽褶/肌理布）；腰部以下为层叠裙摆 |")
w("| 姿态 | 站姿，双手自然背在身后（画上半身时手可省略或轻收入身后） |")
w("| 氛围 | 温和、安静、清爽 |")
w("")

w("---")
w("")
w("## 三、绘图提示词")
w("")
w("### 3.1 中文（即梦 / 文心一格 / 通义万相等）")
w("")
w("```")
w("动漫风格的年轻女性上半身立绘，正面朝前，居中构图。")
w("长卷发过胸，近乎黑色带暖褐色调，中分偏分，额前斜分刘海。")
w("鹅蛋脸，下巴略尖；黑褐色大眼睛，眼神温和；细平眉；淡妆，自然红唇，淡腮红。")
w("肤色偏暖白（#E1C2B4）。")
w("穿白色无袖 V 领连衣裙，上身有细密竖纹肌理，腰部有层叠收褶。肤色裸露的肩与手臂。")
w("背景**纯色或透明**，不要场景、不要文字、不要水印。")
w("扁平插画上色风格，柔和光影，清晰轮廓线，干净简洁。")
w("画幅：竖构图，只画到腰部以上。")
w("```")
w("")
w("### 3.2 英文（Stable Diffusion / Midjourney / NovelAI）")
w("")
w("```")
w("anime style, upper body portrait of a young woman, front facing, centered composition,")
w("long wavy hair past chest, near-black with warm brown tint, side-swept bangs,")
w("oval face, slightly pointed chin, large dark-brown eyes, gentle expression,")
w("thin soft eyebrows, natural light makeup, soft red lips, subtle blush,")
w("warm fair skin, wearing a white sleeveless V-neck dress with fine vertical pleats")
w("and a tiered gathered skirt, bare shoulders and arms,")
w("plain solid background, no scenery, no text, no watermark,")
w("flat illustration coloring, soft shading, clean line art, simple and tidy,")
w("vertical composition, waist-up crop")
w("")
w("# 配色参考（可作 weighted prompt）")
w("hair color #1C1618, skin color #E1C2B4, dress color #D5D8D5")
w("```")
w("")
w("### 3.3 负面提示词（两边都用）")
w("")
w("```")
w("水印, 文字, 签名, logo, 多个角色, 全身, 复杂背景, 真实照片风, 3D渲染,")
w("畸形手指, 多余手臂, 不对称眼睛, 模糊, 低分辨率, 过曝, 死黑块")
w("```")
w("```")
w("watermark, text, signature, logo, multiple characters, full body, complex background,")
w("photorealistic, 3D render, deformed hands, extra arms, asymmetric eyes,")
w("blurry, lowres, overexposed, crushed blacks")
w("```")
w("")

w("---")
w("")
w("## 四、生成时必须满足的**工程约束**")
w("")
w("这几条不是审美偏好，是**为了能接回桌宠程序**。不满足的话，图再好看也用不上。")
w("")
w("| # | 约束 | 为什么 |")
w("|---|------|--------|")
w("| 1 | **只要上半身**，腰部以上 | 桌宠窗口是竖长条，全身会缩到看不清 |")
w("| 2 | **正面或接近正面**朝前 | 程序靠正面像做眨眼/说话的帧替换；侧脸改不了眼和嘴 |")
w("| 3 | **背景纯色或透明** | 要抠图当精灵；杂乱背景抠不干净，边缘会碎 |")
w("| 4 | 头发**不要大面积压住肩线** | 同上，发梢与背景交界处最容易抠碎 |")
w("| 5 | 眼睛与嘴**清晰可辨、不被头发挡** | 这两个部位是动画要单独替换的 |")
w("| 6 | 分辨率 **≥ 1024×1280** | 最终显示只有 127px，但要多帧缩放，源图太小会糊 |")
w("| 7 | 一次生成**同一角色多张**（≥6 张） | 要从里面挑一张眼/嘴位置规整的做动画底图 |")
w("")
w("### 推荐生成参数")
w("")
w("| 项 | 建议 |")
w("|----|------|")
w("| 画幅 | 竖构图 4:5 或 3:4 |")
w("| 采样步数 | ≥ 25（太低线条会断） |")
w("| CFG / 引导强度 | 7~9 |")
w("| 批量 | 6~8 张，挑一张 |")
w("| 若支持 ControlNet / 参考图 | 用照片做 **face/pose 参考但只取大形**，强度调低（0.3~0.5），否则会画成照片风 |")
w("")

w("---")
w("")
w("## 五、验收清单（成品拿到手先过这几条）")
w("")
w("- [ ] 是上半身，腰部以上，没有全身")
w("- [ ] 正面朝前，两眼与嘴都完整可见、没被头发遮住")
w("- [ ] 背景是纯色或透明，边缘干净")
w("- [ ] 发色 / 肤色 / 裙色与配色表对得上（目视近似即可）")
w("- [ ] 没有水印、文字、签名")
w("- [ ] 分辨率 ≥ 1024×1280")
w("- [ ] 头部在画面上方约 1/3 处，留出下方肩与衣服")
w("")
w("不合格就别往下走 —— 拿回去重生成，比后面修补便宜得多。")
w("")

w("---")
w("")
w("## 六、成品接回桌宠的路径（这部分我可以做）")
w("")
w("图拿到后，交给本项目 AI 助手，可以做这些：")
w("")
w("1. **抠图**：环境里有 `rembg`（已装），把背景去掉得到透明 PNG")
w("2. **裁切与缩放**：按 `ui.pet_size`（当前 127）缩放到合适尺寸")
w("3. **做动画帧**：在底图上**局部替换**眼与嘴的位置，生成")
w("   眨眼 / 说话口型的多帧 —— 这是 2D 精灵动画的通行做法，")
w("   本项目 `resources/sprites/cat/` 那 10 个动画就是这个结构")
w("4. **接进程序**：替换 `resources/sprites/<角色名>/` 下的帧，")
w("   或新增一个角色目录，让 `animation.controller` 加载")
w("5. **验收**：跑 `tools/verify_gui_launch.py` 与 `tools/run_acceptance.py` 确认没弄坏别的")
w("")
w("> ⚠️ 第 3 步有个前提：**底图的眼与嘴位置要规整**（正面、对称）。")
w("> 所以上面「工程约束」第 2、5 条必须满足，否则动画做不出来。")
w("")

w("---")
w("")
w("## 七、能力边界（如实记录）")
w("")
w("- 本项目 AI 助手**不能生成图像**：环境里没有 `diffusers`、没有模型权重。")
w("  早期尝试过两条替代路径，都失败并已留档：")
w("  · **代码搭 3D VRM** → `gen_chibi_vrm.py` 生成的模型**不做蒙皮**")
w("    （0 个 skin、47 个网格全是静态），骨骼驱动不了它，装不上。")
w("    留档：`evidence/p5/vrm_facing.txt`")
w("  · **代码画 2D 插画** → 几何多边形拼出来的图棱角明显、比例生硬，")
w("    达不到可用品质。留档：`evidence/p5/_bust_sheet.png`")
w("- 因此**画形象这件事必须由外部工具或人完成**。这份规格书就是为此准备的中间产物。")
w("- 规格书里的描述来自照片的**客观特征与取色**，不含任何「AI 想象中的长相」。")
w("")

OUT.parent.mkdir(parents=True, exist_ok=True)
OUT.write_text("\n".join(L) + "\n", encoding="utf-8")
print(f"写出 {OUT.relative_to(ROOT)}（{OUT.stat().st_size} bytes，{len(L)} 行）")
